# DiffDeck bug bash — 2026-07-16

Status: complete; zero open findings

## Environment and method

- macOS, Git, Bun 1.3.14, Node 20.20.2 and Node 24.15.0.
- Playwright 1.61.1 with Chromium, Firefox, and WebKit; Axe 4.12.1.
- Disposable `unborn`, `empty`, `review`, `conflict`, `edge`, and `huge-count` repositories.
- The built adapter—not a mock—parsed the edge and 23,000-file fixtures. The installed npm tarball
  was exercised through its real `diffdeck` bin symlink.
- Findings use P0–P3 severity and remain in this log after closure. A closed finding has a durable
  regression at the lowest useful layer.

## Pass record

| Pass | Scope exercised | Evidence | Result |
| --- | --- | --- | --- |
| P1 static/architecture | Input-to-Git/filesystem/process/URL/clipboard/write traces; effects, aborts, listeners, timers; five largest files | `docs/research/architecture-bug-bash-2026-07-16.md`, TypeScript, Oxlint, coverage | Closed |
| P2 CLI/Git/package | Every CLI parser branch, worktree/cached/commit/range, unusual paths, conflict, binary, type change, Node 20/24 tarball | `tests/cli.test.ts`, `tests/git.test.ts`, `scripts/verify-built-fixtures.mjs`, `scripts/package-smoke.mjs` | Closed |
| P3 server/security/write | All read/write/image/SSE/editor/structural/packet routes; tokens, traversal, symlink escape, body/output bounds, stale snapshots, whole-file/hunk mutations | `tests/server.test.ts` | Closed |
| P4 review/refresh | Corrupt/migrated state, rename/remove/shift/ambiguity reconciliation, file/line notes, drafts, filters and preferences | `tests/reviewSession.test.ts`, `tests/reviewPacket.test.ts`, QOL browser tests | Closed |
| P5 browser/responsive | Pointer and keyboard review paths, palette, dynamic 1440→320→1280→600 resize, persisted layout/preferences, 23,000-file list | `e2e/chromium.e2e.ts`, `e2e/core.e2e.ts` | Closed |
| P6 accessibility | Axe representative states, keyboard patch navigation/comments, tree/list semantics, modal isolation, forced colors, reduced motion, 200% equivalent viewport | E2E suite and visual baselines | Closed |
| P7 performance/resilience | Lazy request cancellation, focus-mode request isolation, DOM/request caps, huge adapter timing, fatal/recoverable session failures, repeated shutdown | Browser diagnostics, built-fixture gate, package smoke | Closed |

## Finding ledger

| ID | Sev. | Reproduction and root cause | Resolution | Regression evidence | Status |
| --- | --- | --- | --- | --- | --- |
| NBB-001 | P1 | Resize the live desktop workspace through the narrow breakpoint; a still-mounted virtual surface could retain invalid dimensions and show a blank patch. | Narrow widths now switch between complete Files and Review places; tree lifecycle fixes were upgraded upstream. | `dynamic desktop-to-phone resize keeps a usable patch…` | Closed |
| NBB-002 | P2 | The palette said “Next / previous file” but only advanced. The label combined two actions around one callback. | Separate previous/next visible and unviewed commands with honest disabled boundaries. | `tests/reviewNavigation.test.ts`, three-engine core navigation | Closed |
| NBB-003 | P2 | Reset and revert used native confirmation while other destructive actions used app UI. | Shared modal confirmation, Cancel-first focus, Escape, exact scope, trigger restoration. | `destructive confirmations are modal…` | Closed |
| NBB-004 | P1 | A deep-linked line could lose ownership while virtual cards mounted and update the URL to another visible file. | Pinned visible-path ownership until the requested card is ready; bounded focus/scroll retry. | `tests/visiblePath.test.ts`, deep-link tests | Closed |
| NBB-005 | P1 | Global comment shortcut targeted hunk context rather than its first changed line. | Hunk targets are derived from the first side-specific addition/deletion. | `tests/hunkNavigation.test.ts` | Closed |
| NBB-006 | P1 | Automated accessibility inspection found invalid tree ownership and non-focusable scrollable code. | Correct tree children/levels plus labelled, keyboard-scrollable code regions. | Axe core/visual checks | Closed |
| NBB-007 | P2 | Status/diff colors and secondary copy missed AA in some light/dark combinations. | Consolidated high-contrast OKLCH roles and explicit forced-colors behavior. | `DESIGN.md` contrast table, visual/Axe suite | Closed |
| NBB-008 | P1 | Refresh/preference failures could replace usable review content with a fatal shell. | Existing content remains mounted; failures explain recovery, while first-load failure has Retry. | `session and preference failures keep a clear recovery path`, error visual | Closed |
| NBB-009 | P1 | Editor path authorization used lexical containment only; an in-repo symlink could escape the repository. | Existing paths also require realpath containment. | `rejects editor traversal, symlink escape…` | Closed |
| NBB-010 | P2 | The empty state lacked a page heading and conflated empty diff with filtering. | Semantic `h1` and distinct actionable empty/filter states. | Empty visual + Axe | Closed |
| NBB-011 | P1 | The accessible patch counted notes but did not expose existing note bodies/statuses. | It now renders the shared file/line note model with textual status. | accessible-note E2E and dark note visual | Closed |
| NBB-012 | P2 | Dark review mode left Sonner in its light theme. | Toaster follows the persisted application theme. | `comment-dark.png` | Closed |
| NBB-013 | P1 | Real unresolved `diff --cc` output was passed to a unified parser and crashed the session. | Combined blocks are excluded from unified parsing; unresolved contents remain a first-class conflict state. | real merge in `tests/git.test.ts`, built conflict fixture | Closed |
| NBB-014 | P1 | Opening the palette with 23,000 files mounted every file command and blocked input. | React prefilter plus a 100-result cmdk window and honest truncation status. | 23,000-file browser test; <150 cmdk items | Closed |
| NBB-015 | P1 | cmdk's generated `aria-labelledby` overrode the input `aria-label`, announcing the search field as “DiffDeck commands.” | The cmdk root label is now the intended search-field name. | role/name lookup in 23,000-file E2E | Closed |
| NBB-016 | P1 | An unstaged regular-file→symlink change produced duplicate entries; hydration followed the link and could render target contents. | Read link values without following targets and coalesce delete/add mode blocks into one modified diff. | symlink target non-disclosure test, built edge fixture | Closed |
| NBB-017 | P2 | The adversarial fixture created huge new files without intent-to-add, so Git never sent them to the parser. | Added intent-to-add and assertions for huge files, added binary, quoted path, copy, and dirty submodule. | `verify:fixtures`, `verify:built-fixtures` | Closed |
| NBB-018 | P2 | Existing accessible notes were invisible after switching surfaces, and aborted offscreen requests were reported as network failures. | Shared notes render on both surfaces; diagnostics distinguish intentional lazy-surface aborts from failures. | accessible-note E2E with clean diagnostics | Closed |
| NBB-019 | P3 | Resize E2E queried an unmounted Virtuoso row and timed out despite a healthy list. | Test now uses the visible file-search interaction before selecting the virtual row. | focused resize test | Closed |
| NBB-020 | P1 | Installed package crashed on Node 20 because Pierre's root export evaluated `navigator.userAgent`; Node 24 masked it. | A server-only inert Navigator compatibility shim runs before parser import when the global is absent. | Node 20 actual-bin package smoke, CI Node 20/24 matrix | Closed |
| NBB-021 | P2 | Command-palette stress locator could not find the visible input because the test assumed the wrong semantic role before the accessible-name defect was understood. | Trace inspection corrected the product label and retained a role-based combobox locator. | palette accessibility test | Closed |
| NBB-022 | P2 | File/image fetches aborted by an intentional surface unmount were treated as unexplained failed requests. | Only GET aborts for lazy diff/image surfaces and SSE teardown are exempt; all other failures still fail E2E. | shared diagnostics fixture | Closed |

## Closure statement

Open findings by severity: P0 0, P1 0, P2 0, P3 0. The two original seed defects and every
finding discovered during implementation are closed. Infrastructure defects are retained because a
false-green or false-red test weakens the same release boundary as a product defect.
